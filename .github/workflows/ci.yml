name: CI

on:
  push:
    branches: [master, main]
  pull_request:

# Least privilege: CI only reads the repo. It never writes contents, comments,
# or statuses, so drop the default read/write token down to read-only.
permissions:
  contents: read

# Don't pile up redundant runs. A rapid re-push to the SAME ref supersedes the
# previous run — but only cancel PR runs; never cancel an in-flight run on
# main (we want every pushed commit on a protected branch fully verified).
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  verify:
    name: typecheck • test • build
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Build
        run: npm run build

  e2e:
    name: e2e smoke (Playwright)
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Build
        run: npm run build

      - name: E2E smoke
        run: npx playwright test

      - name: Upload traces on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: playwright-traces
          path: test-results/
          retention-days: 7

  depcheck:
    name: depcheck (advisory)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # Advisory only — flags unused deps + phantom (undeclared) imports but must
    # never block a merge. We deliberately stayed on npm rather than pnpm; this
    # is the cheap guardrail that recovers pnpm's one real benefit (catching
    # hidden dependencies) without the migration cost.
    continue-on-error: true

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22'
          cache: 'npm'

      # depcheck resolves a few packages (peer deps, special parsers) via the
      # installed tree, so install first for results that match a local run.
      - name: Install
        run: npm ci

      - name: Check for unused / missing dependencies
        shell: bash
        run: |
          echo '## depcheck (advisory)' >> "$GITHUB_STEP_SUMMARY"
          echo '' >> "$GITHUB_STEP_SUMMARY"
          echo 'Flags unused dependencies and phantom (undeclared) imports. Advisory only — does not block merge.' >> "$GITHUB_STEP_SUMMARY"
          echo '' >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          # Pinned to the version validated locally. `|| true` keeps the step
          # green: depcheck exits non-zero on any finding, but this is advisory.
          npx --yes depcheck@1.4.7 2>&1 | tee -a "$GITHUB_STEP_SUMMARY" || true
          echo '```' >> "$GITHUB_STEP_SUMMARY"
